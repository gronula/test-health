include ../utils/mixins
include blocks

block variables

doctype html
html(lang='ru')
  include head

  body.page(class=`page--${htmlWebpackPlugin.options.title}`)
    block content
    block scripts
